import { DicomComparisonEntry } from '../model/dicom-entry';
import { HeavyweightFile } from '../model/file-interfaces';

export function filesToComparisonData(files: HeavyweightFile[], colours: string[]): DicomComparisonEntry[] {
    let comparisonEntries: DicomComparisonEntry[] = [];

    files.map((file, index) => {
    });
    return comparisonEntries;
} 
