import 'mocha';
import { expect } from 'chai';

describe("Sample test", () => {
    it("Fails", () => {
        // given

        // when 
        

        // then
        expect(1).to.be.equal(2);

    });
});