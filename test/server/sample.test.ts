import 'mocha';
import { expect } from 'chai';

describe("Sample test server", () => {
    it("Passes", () => {
        // given

        // when 
        

        // then
        expect(1).to.be.equal(1);

    });
});