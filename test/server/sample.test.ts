import 'mocha';
import { expect } from 'chai';

describe("Sample test", () => {
    it("Passes", () => {
        // given

        // when 
        

        // then
        expect(1).to.be.equal(1);

    });
});